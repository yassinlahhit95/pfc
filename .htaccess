# ══════════════════════════════════════════════════════════════════════════════
# AULAPRO - CONFIGURACIÓN DE RENDIMIENTO Y SEGURIDAD
# ══════════════════════════════════════════════════════════════════════════════

# 1. COMPRESIÓN GZIP (Reduce el tamaño de transferencia hasta un 70%)
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/plain
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/xml
    AddOutputFilterByType DEFLATE text/css
    AddOutputFilterByType DEFLATE application/xml
    AddOutputFilterByType DEFLATE application/xhtml+xml
    AddOutputFilterByType DEFLATE application/rss+xml
    AddOutputFilterByType DEFLATE application/javascript
    AddOutputFilterByType DEFLATE application/x-javascript
    AddOutputFilterByType DEFLATE image/svg+xml
</IfModule>

# 2. CACHÉ DEL NAVEGADOR (Evita descargas redundantes en visitas recurrentes)
<IfModule mod_expires.c>
    ExpiresActive On
    
    # Imágenes (1 año)
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/webp "access plus 1 year"
    ExpiresByType image/x-icon "access plus 1 year"
    
    # Fuentes (1 año)
    ExpiresByType font/ttf "access plus 1 year"
    ExpiresByType font/otf "access plus 1 year"
    ExpiresByType font/woff "access plus 1 year"
    ExpiresByType font/woff2 "access plus 1 year"
    
    # CSS y JS (1 mes)
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
    ExpiresByType text/javascript "access plus 1 month"
    
    # Video (1 año)
    ExpiresByType video/mp4 "access plus 1 year"
    ExpiresByType video/webm "access plus 1 year"
</IfModule>

# 3. SEGURIDAD
Options -Indexes

# Bloquear acceso directo a archivos sensibles (Apache 2.4)
<IfModule mod_authz_core.c>
    <FilesMatch "(\.env|composer\.(json|lock)|database\.sql|\.htpasswd|\.gitignore)$">
        Require all denied
    </FilesMatch>
</IfModule>

# Bloquear acceso directo a carpetas internas
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(config|include|modelos|templates|logs|vendor)(/|$) - [F,L]
</IfModule>

# Páginas de error genéricas (sin revelar 403 vs 404)
# Uses <If> to work both at domain root (server) and /pfc/ subdirectory (local Laragon)
<IfModule mod_rewrite.c>
    <If "%{REQUEST_URI} =~ m|^/pfc/|">
        ErrorDocument 400 /pfc/vistas/error.php
        ErrorDocument 403 /pfc/vistas/error.php
        ErrorDocument 404 /pfc/vistas/error.php
        ErrorDocument 500 /pfc/vistas/error.php
    </If>
    <Else>
        ErrorDocument 400 /vistas/error.php
        ErrorDocument 403 /vistas/error.php
        ErrorDocument 404 /vistas/error.php
        ErrorDocument 500 /vistas/error.php
    </Else>
</IfModule>

# Cabeceras de seguridad HTTP
<IfModule mod_headers.c>
    Header set X-Frame-Options "SAMEORIGIN"
    Header set X-Content-Type-Options "nosniff"
    Header set X-XSS-Protection "1; mode=block"
    Header set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>

# 4. PROTECCIÓN CONTRA BOTS / DDoS (nivel Apache)
# Limitar tamaño máximo de cuerpo POST (100 KB) — evita body floods
LimitRequestBody 102400

# Bloquear User-Agents típicos de bots maliciosos y scanners
<IfModule mod_rewrite.c>
    RewriteCond %{HTTP_USER_AGENT} (libwww-perl|wget|python|nikto|curl|scan|java|winhttp|clshttp|loader) [NC]
    RewriteRule .* - [F,L]
</IfModule>

# Limitar métodos HTTP a los necesarios
<LimitExcept GET POST HEAD>
    Require all denied
</LimitExcept>

# Bloquear peticiones sin User-Agent (la mayoría de bots primitivos)
<IfModule mod_rewrite.c>
    RewriteCond %{HTTP_USER_AGENT} ^-?$
    RewriteRule .* - [F,L]
</IfModule>

# Rate limiting con mod_evasive (si está instalado en producción)
# En producción instalar: sudo apt install libapache2-mod-evasive
<IfModule mod_evasive24.c>
    DOSHashTableSize    3097
    DOSPageCount        10
    DOSSiteCount        50
    DOSPageInterval     1
    DOSSiteInterval     1
    DOSBlockingPeriod   600
</IfModule>
